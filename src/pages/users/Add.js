// UserForm import
import UserForm from "../../components/UserForm";

// Returns the UserForm component (no props passed since this is a new user)
const Add = () => {
	return (
		<>
			<UserForm/>
		</>
	);
};

export default Add;
