// GameForm import
import GameForm from "../../components/GameForm";

// Returns the GameForm component (no props passed since this is a new game)
const Add = () => {
	return (
		<>
			<GameForm/>
		</>
	);
};

export default Add;
